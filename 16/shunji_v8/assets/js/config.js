/* ============================================================
   順吉 SHUNJI — 設定檔
   只有這一個檔案需要改。改完就能上線。
   ============================================================ */

window.SHUNJI_CONFIG = {

  /* --- 1. LINE 驗證（必填）-------------------------------------------------
     到 https://developers.line.biz/console/
     建立 LINE Login channel → LIFF 分頁 → Add → 複製 LIFF ID 貼在這裡。
     格式長這樣：2006123456-AbCdEfGh
     留空 = 測試模式（用假的 LINE 帳號跑流程，方便你先看畫面）。          */
  LIFF_ID: '',

  /* 進入點餐頁時，是否自動跳出 LINE 驗證。
     true  = 一進點餐頁就跳（你要的行為）
     false = 讓客人先看菜單，按「加入訂單」時才跳                          */
  AUTO_PROMPT_LOGIN: true,


  /* --- 2. 訂單要送到哪裡（選填，但沒設就只會存在客人手機裡）--------------
     兩種做法擇一，把網址貼進來：
     (a) 免伺服器：server/gas/Code.gs 部署成 Google Apps Script 網頁應用程式
     (b) 自架：server/node 跑起來，填 https://你的網域/api/orders            */
  ORDER_WEBHOOK_URL: '',


  /* --- 3. 官方帳號（選填）-------------------------------------------------
     填了之後，尚未加好友的客人送單後會看到「加入官方帳號接收通知」。       */
  OA_ADD_FRIEND_URL: '',   // 例：https://line.me/R/ti/p/@yourshop


  /* --- 4. 店家資訊 -------------------------------------------------------- */
  SHOP: {
    name:     '順吉',
    nameEn:   'SHUNJI',
    tagline:  '定食・丼飯・拉麵',
    address:  '台北市大安區信義路四段 123 號',
    phone:    '02-2700-1234',
    hours:    '11:00 – 14:30 / 17:00 – 21:00（週一公休）',
    mapUrl:   'https://maps.google.com/?q=台北市大安區信義路四段123號',
    currency: 'NT$'
  },

  /* --- 5. 其他 ----------------------------------------------------------- */
  DEV_MOCK_LOGIN: true,          // LIFF_ID 留空時，允許用假帳號測試流程
  SEND_LINE_MESSAGE_ON_SUBMIT: true  // 在 LINE 內開啟時，把訂單摘要送進聊天室
};
